---
name: init
description: Safely prepare a Ralph Loop worktree.
version: 1
---

# Init

- Start with `./ralph-loop schema init --output json`.
- Run `./ralph-loop init --dry-run --output json` before the real command.
- Use `--json` when constructing requests from the schema.
